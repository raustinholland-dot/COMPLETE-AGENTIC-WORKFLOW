# ─────────────────────────────────────────────
# Clearwater Deal Intelligence Engine — Environment Variables
# Copy this file to .env and fill in all values before running docker compose up
# NEVER commit .env to GitHub
# ─────────────────────────────────────────────

# ── n8n ──────────────────────────────────────
N8N_HOST=localhost
N8N_PROTOCOL=http
WEBHOOK_URL=http://localhost:5678/
# Generate a random 32-char string: openssl rand -hex 16
N8N_ENCRYPTION_KEY=REPLACE_WITH_RANDOM_32_CHAR_STRING

# ── Database ─────────────────────────────────
# Generate a strong password: openssl rand -base64 24
POSTGRES_PASSWORD=REPLACE_WITH_STRONG_PASSWORD

# ── Redis ─────────────────────────────────────
REDIS_PASSWORD=REPLACE_WITH_REDIS_PASSWORD

# ── AI APIs ───────────────────────────────────
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...

# ── Gmail OAuth2 (for ingestion inbox) ────────
# Set up at: https://console.cloud.google.com
# Scope: https://www.googleapis.com/auth/gmail.readonly
GMAIL_CLIENT_ID=REPLACE_WITH_GMAIL_CLIENT_ID
GMAIL_CLIENT_SECRET=REPLACE_WITH_GMAIL_CLIENT_SECRET
GMAIL_INGESTION_ADDRESS=REPLACE_WITH_DEDICATED_GMAIL_ADDRESS

# ── Gmail OAuth2 (for sending outputs) ────────
GMAIL_SEND_CLIENT_ID=REPLACE_WITH_SEND_GMAIL_CLIENT_ID
GMAIL_SEND_CLIENT_SECRET=REPLACE_WITH_SEND_GMAIL_CLIENT_SECRET

# ── Google Calendar ───────────────────────────
# Same OAuth2 app or separate; scope: https://www.googleapis.com/auth/calendar.readonly
GOOGLE_CALENDAR_CLIENT_ID=REPLACE_WITH_CALENDAR_CLIENT_ID
GOOGLE_CALENDAR_CLIENT_SECRET=REPLACE_WITH_CALENDAR_CLIENT_SECRET
GOOGLE_CALENDAR_ID=primary

# ── Salesforce (future use) ───────────────────
SALESFORCE_CLIENT_ID=
SALESFORCE_CLIENT_SECRET=
SALESFORCE_INSTANCE_URL=https://YOURORG.salesforce.com

# ── Delivery ──────────────────────────────────
# Your Outlook address — all AI-generated outputs are sent here
AUSTIN_EMAIL=austin@clearwaterits.com

# ── GitHub MCP ───────────────────────────────
GITHUB_TOKEN=ghp_...
GITHUB_REPO=REPLACE_WITH_YOUR_GITHUB_REPO
